Infrastructure implementations that satisfy domain interfaces.

**Key elements:**
- `unit_of_work.py`: Context-manager for psycopg2 transactions.
- `repositories/`: Concrete adapters for persistence.

